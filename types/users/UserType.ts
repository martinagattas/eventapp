export type LoginType = {
    email: string;
    password: string;
}

export type RegisterType = {
    username: string;
    firstname: string;
    lastname: string;
    email: string;
    password: string;
    confirmPassword: string;
    type: string;
}